import React, { useEffect, useState } from 'react';
import { Table } from 'react-bootstrap';

const OrderDetails = (props) => {
    const { _id, name, email} = props.order


    return (
        <div>
           
        </div>
    );
};

export default OrderDetails;